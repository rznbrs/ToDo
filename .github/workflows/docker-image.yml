name: Build and Push Docker Images

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build Maven package
        run: mvn clean package -DskipTests

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push backend Docker image
        run: |
          docker build -t kobra0000/tobo-backend:latest .
          docker push kobra0000/tobo-backend:latest

      - name: Build and push frontend Docker image
        run: |
          docker build -t kobra0000/todo-frontend:latest C:\Users\rezan\OneDrive\Masaüstü\todo-frontend
          docker push kobra0000/todo-frontend:latest
